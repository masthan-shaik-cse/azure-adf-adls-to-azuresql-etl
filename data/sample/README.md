# Retail Sample Data

Run `python scripts/generate_sample_data.py` from the repository root to generate `customers.csv` and `orders.json`.

Run `python scripts/generate_parquet_with_pyspark.py` in Databricks or a local PySpark environment to generate `payments_parquet/`.
